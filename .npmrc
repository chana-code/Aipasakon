# vitest@2.1.9 pins vite@5, while @vitejs/plugin-react@6 wants vite@8.
# These are test-only devDeps and never enter the production `next build`
# bundle, so allow the peer mismatch (npm 10 otherwise hard-fails ERESOLVE
# on CI). Mirrors the working local install.
legacy-peer-deps=true
